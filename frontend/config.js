/* Flip useMock to false when the backend is running.
   apiBase must match the server the backend developer starts. */
const KSConfig = {
  useMock: false,
  apiBase: "https://kissan-setu-backend.onrender.com/api/v1",
  lang: "en"
};
